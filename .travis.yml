---
language: ruby
rvm:
  - 2.4.5
  - 2.5.3
  - 2.6.0

before_install:
  - gem install bundler --version 2.0.1

script:
  - bundle exec rspec
  - bash <(curl -s https://codecov.io/bash)
#  - bundle exec rubocop
